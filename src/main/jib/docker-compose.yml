version: '3.8'
services:
  app:
    image: com.example/oltp:0.0.1-SNAPSHOT  # Replace with your actual image name and tag
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=qa
